# chocs-api-status
Middleware providing default status monitoring endpoint

## Installation 
through poetry:
```shell
poetry add chocs-middleware.api-status
```
or through pip:
```shell
pip install chocs-middleware.api-status
```

